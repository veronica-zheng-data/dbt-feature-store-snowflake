version: 2

seeds:
  - name: customers
    description: A mapping of two letter country codes to country names
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: first_name
        tests:
          - not_null
      - name: last_name
        tests:
          - not_null